import { useCallback, useEffect, useState } from "react"; import { Link, useParams } from "react-router-dom"; import { ErrorState, LoadingState } from "../components/AsyncState"; import { useCaseContext } from "../context/CaseContext"; import { getAlerts, getCaseDetail } from "../services/api"; import type { AlertItem } from "../types/analytics"; import type { CaseSummary } from "../types/workspace";
export function CaseOverviewPage(){const {caseId:routeId}=useParams();const id=Number(routeId)||1;const {setCaseId}=useCaseContext();const [item,setItem]=useState<CaseSummary|null>(null);const [alerts,setAlerts]=useState<AlertItem[]>([]);const [error,setError]=useState(false);const load=useCallback(()=>{setError(false);Promise.all([getCaseDetail(id),getAlerts(id)]).then(([caseItem,items])=>{setItem(caseItem);setAlerts(items);setCaseId(id);}).catch(()=>setError(true));},[id,setCaseId]);useEffect(load,[load]);if(error)return <ErrorState label="Unable to load case overview." retry={load}/>;if(!item)return <LoadingState/>;return <section className="page"><div className="breadcrumbs"><Link to="/cases">Cases</Link><span>/</span><span>{item.title}</span></div><header className="page-header"><div><p className="eyebrow">{item.case_number}</p><h1>{item.title}</h1><p className="muted">{item.description}</p></div><span className={`status-pill ${item.status}`}>{item.status}</span></header><div className="metric-grid">{Object.entries(item.metrics??{}).map(([label,value])=><div className="metric" key={label}><span>{label}</span><strong>{value}</strong></div>)}</div><section className="veil-panel"><div className="panel-head"><h2>Investigation paths</h2></div><div className="panel-body quick-links"><Link className="veil-button" to="/network">Explore network</Link><Link className="veil-button secondary" to="/timeline">View timeline</Link><Link className="veil-button secondary" to="/map">View map</Link><Link className="veil-button secondary" to="/documents">View documents</Link></div></section><section className="veil-panel"><div className="panel-head"><h2>Open analytical alerts</h2><Link to="/alerts">View all</Link></div><div className="panel-body stack-list">{alerts.filter(row=>row.status==="OPEN").slice(0,6).map(row=><div className="stack-row" key={row.id}><span className={`status-pill ${row.severity}`}>{row.severity}</span> {row.title}<small className="muted"> · Entity P{String(row.entity_id??0).padStart(3,"0")} · {row.score.toFixed(0)}/100</small></div>)}</div></section></section>}
